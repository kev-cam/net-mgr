<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- BLE runtime permissions, split by API level:
         Android 12+ (API 31): BLUETOOTH_SCAN / _CONNECT / _ADVERTISE are
         runtime perms. neverForLocation on SCAN so we don't get pushed
         into needing ACCESS_FINE_LOCATION on modern devices.
         Android 11 and below: fall back to legacy BLUETOOTH + BLUETOOTH_ADMIN
         plus ACCESS_FINE_LOCATION (which BLE scan needs pre-12). -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"
        tools:targetApi="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="31" />

    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <!-- Foreground service — Android 14+ needs the connectedDevice
         type declaration BOTH here and on startForeground(). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE"
        tools:targetApi="34" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"
        tools:targetApi="33" />

    <!-- Declare BLE requirement so the app only lists on Play for BLE
         devices. Not required=false so a phone without BLE still runs
         (the operator just gets a runtime error). -->
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="false" />

    <!-- allowBackup=false: the app now persists the raw 32-byte identity
         seed in SharedPreferences (see MainActivity.loadOrCreateIdentity).
         That seed reconstructs the Ed25519 signing key, the Noise static
         key, and the peer_id, so it must not be swept into Android Auto
         Backup / `adb backup`. Before identity was persisted there was no
         key material to protect; the persistence is what creates the path. -->
    <application
        android:allowBackup="false"
        android:label="net-chat"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".BitChatService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />
    </application>
</manifest>
